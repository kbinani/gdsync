# frozen_string_literal: true

source 'https://rubygems.org'

gem 'google_drive'
gem 'win32-file-attributes' , platforms: [:mswin, :mingw, :mswin64, :x64_mingw]
gem 'coveralls', require: false

group :development, :test do
  gem 'test-unit'
  gem 'simplecov'
end
